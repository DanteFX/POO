public String toString1(){
  return nombre+" "+super.getPatas()+" "+pensante;

  public boolean equals(Objet o){
    if (!(o instanceof Persona)){
      return false;
    }
  }
  ((Persona)tmp).o();
  if (nombre.equals(tmp.nombre)&&(patas==tmp.patas)&&(pensante==tmp.))
}
